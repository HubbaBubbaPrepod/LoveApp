# fastlane/Appfile

# Android package name (matches applicationId in build.gradle.kts)
package_name("com.example.loveapp")

# Google Play service-account JSON key used by supply
json_key_file(ENV["PLAY_STORE_JSON_KEY"] || "fastlane/play-store-key.json")
